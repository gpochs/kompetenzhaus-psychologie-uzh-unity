# Kompetenzhaus WebGL shell

The custom template is a semantic DOM interface over the actual Unity game. Unity owns learning progress, curriculum projection, quiz shuffling/correctness, architecture validation and persistence. `shell.js` sends commands and renders returned snapshots. It never awards progress or calculates an alternative competency score.

## Build integration

Copy `index.html`, `shell.css`, `shell.js`, `bridge-contract.mjs`, `framework-view.mjs` and `shell-locale.mjs` together into the Unity custom WebGL template. Unity substitutes the standard loader/data/framework/code filename and product placeholders in `index.html`.

The deployed directory also needs:

- `content/kompetenzhaus-content.json` (canonical public source export)
- `content/module-learning-design.json` (current canonical module proposals)
- `companion/published.json` (verified publication metadata)
- `companion/context-catalog.json` (public safe context catalogue)
- `TemplateData/cover.png` (optional generated cover; used only in the labelled welcome artwork)

Override resource paths through `window.KOMPETENZHAUS_CONFIG` before the module loads if deployment uses another layout. Optional keys: `contentUrl`, `moduleDesignUrl`, `coverUrl`, `companionUrl`, `companionEmbedUrl`, `companionPublicationUrl`, `companionCatalogUrl`.

The root Unity scene must contain the `KompetenzhausBridge` GameObject and its `ReceiveCommand` method. The embedded development `PrototypeHud` should be disabled in the final scene so it does not duplicate this DOM UI.

Do not deploy `shell-preview.html`, `preview-fixture.mjs`, `preview-framework.mjs`, `qa/`, or the tests. `?shell-preview=1` intentionally suppresses Unity loading and visibly labels the UI as a preview. The standalone preview adds explicit development controls and never represents itself as a functioning 3D game. The `qa/framework-v2-*.json` files are actual C# JsonUtility projection exports from explicitly synthetic game states. They contain proposed opportunities and no assessment records. The older legacy QA files are retained for regression history and are not consumed by the current preview.

## Contract

`window.KompetenzhausBridge.receive(json)` consumes schema 1 host snapshots. `window.KompetenzhausBridge.send(command,payload)` sends a whitelisted `{command,payload}` JSON envelope to `unityInstance.SendMessage('KompetenzhausBridge','ReceiveCommand', ...)`. `window.KompetenzhausShell.attach(instance)` establishes the transport and requests the initial snapshot.

Questions use the host's shuffled display order. Selection is local until **Antwort prüfen**. Feedback remains until **Weiter**, including the final question. **Complete** uses `closeQuiz`, never `continue`. The complete host explanation is displayed, and the feedback section is scrolled into view. Opening a native dialog moves focus into it, traps Tab, supports Escape, and freezes Unity controls through `setUiFocus`.

Architecture transactions clone the returned state and submit a complete candidate for host validation. Grid bounds are −4…3, floors 0…3, 4 m per bay and 64 floor tiles per house across all floors. Decorations use local metres and follow room moves by `delta * gridSizeMetres`. Undo concerns architecture only. Built modules stay in the host inventory when a plan is replaced.

The connection editor offers doors only across a shared same-floor wall. Stairs connect different rooms at identical x/z coordinates on consecutive floors. Both endpoint cells must be clear of module footprints, furniture centres and other stair endpoints. Candidate filtering never deletes furniture. Removing a connection is a separate architecture transaction. Floor and roof controls send `setArchitectureView` to keep the cutaway aligned with the selected plan floor.

The optional **Studium** menu contains the module catalogue, all canonical competencies and the activity-based career compass. `framework-view.mjs` renders only `snapshot.frameworkProfile`: canonical criteria, task levels I–III, proposed tasks/evidence/success criteria, and 14 Future Skills / AI perspectives. A perspective filters the same canonical criteria and matching task contexts; it adds no points. The 12 career views show activities, proposed opportunities and next modules returned by Unity. Optional digital contexts are labelled optional. Each competency can display its source-authored AI role, learner contribution, proposed assessment focus and example task through `aiIntegration`; these use the same canonical criterion IDs. Competency counts and headings are read from the host, so the V2.1 technical split does not require hardcoded UI totals. Personal competence remains unassessed. Quiz/quest practice is shown separately. Missing V2 data produces an unavailable message, never legacy levels, radar, percentages or career-fit scores. `snapshot.profile.credits` is used only for academic module-credit totals. Source-derived topic/module choices in the existing module UI remain available.

The entry guide offers a first room, an optional editable preset or a direct source quiz. Its navigation helper reads Unity eligibility and placements; it cannot award achievements. After a quiz on an empty plot, the build action opens the room editor. Room name and size are immediate; plan drawing, floors, geometry and connections are expandable. The room commit and next learning action remain visible in the builder footer. Optional `snapshot.journey` from Unity adds the current main quest with purpose, instructions and location; it does not lock free building. The profile is independently loaded through the V2 host snapshot.

The AI iframe is created lazily only on explicit opening and only on a hostname allowed in publication metadata. A public open link remains available. Context copy is explicit; the v3 envelope includes only known active-mode module IDs/choices, known quiz/quest practice IDs and an allowlisted framework version. It never exports legacy competence levels, field scores or career-fit percentages. It omits names, room labels, notes and conversation text. No automatic cross-frame transfer is claimed or attempted. Localhost deliberately shows the separate public link instead of an unauthorized embed.

## Design and verification

Visual direction: `art/references/game-ui-concept.png`, inspected before UI implementation. Warm ivory surfaces, navy Georgia headings, restrained oak/teal accents, slim left build palette and navy lower navigation. Intentional changes from the generated concept: real source titles/counts, the optional Studium drawer, scrollable long questions, a unique house glyph instead of an institutional crest, accessible native controls, and a live Unity canvas rather than the concept image as a game background.

Browser checks on 12 September 2026: desktop welcome and canvas overlay composition; narrow mobile layout; immediate quiz foreground; disabled check before selection; selection-to-feedback flow; final explanation visibly retained; explicit continuation to completion and separate return to house. Screenshots were visually compared to the concept in the browser tool. The local preview is a labelled UI fixture; full Unity rendering and the public Claude iframe require separate integrated-build verification.

V2.0 browser checks on 12 September 2026: the clearly labelled DOM fixture displayed all 15 competencies and 12 roles; selecting a Future Skills perspective reduced the view to the referenced canonical criteria. Proposed evidence and task levels appeared under their actual labels. English labels rendered correctly. A 390 × 844 responsive override produced a 283 CSS px dialog with equal clientWidth and scrollWidth; the profile remained scrollable without horizontal overflow. The test-harness toolbar was moved below the iframe so it could not obscure mobile controls. The browser override was reset. Missing V2 data visibly produced the unavailable message. The actual C# selected-options fixture additionally rendered Mentoring 505 under K2.1/K2.3 with its proposed reflection task, evidence and without-AI context. These checks concern DOM behaviour and do not establish Unity 3D-world behaviour. The final V2.1 checks are documented below.

Additional browser checks exercised creation of ground- and upper-floor rooms, a stair whose valid endpoint count changed from four to three and back to four after deletion, and a door between neighbouring ground-floor rooms. Semester/stage metadata and separately labelled game-model prerequisites were visible in the module catalogue. These are DOM/transport fixture checks; stair mesh traversal still needs the actual Unity scene.

The entry-guide browser checks covered direct quiz entry on an empty plot, the return to room creation, the first room transaction, the resulting next-module prompt, and an editable courtyard preset. The new guide was checked on a narrow viewport (about 303 CSS px card width) without horizontal overflow; its main action remained visible. A preview-only label was made pointer-transparent after it obscured a close control in this test harness. No live 3D-world claim is made.

Run `node --test web-template/contract.test.mjs web-template/framework-view.test.mjs` from the repository root. Seventeen tests cover host-derived guidance without progression mutation, transport/phase boundaries, displayed answer indexes, invalid snapshots, safe URLs, upper-floor support, furnished room moves, door adjacency, stair geometry/occupancy/endpoint reservations, the exported v3 tutor context against the actual companion validator, rejection of legacy score fallback, and context filtering without changing canonical criteria or task levels. `node --check` validates `shell.js`, `shell-locale.mjs`, `framework-view.mjs`, `preview-fixture.mjs` and `preview-framework.mjs`.

### Final V2.1 checks — 12 September 2026

The current actual C# JsonUtility fixtures for empty, all-modules and selected-options states were loaded in the Edge development preview. The all-modules profile renders 16 competency disclosures, 48 canonical criteria and 16 complete AI cards; every competency disclosure was opened. T2 (existing-tool use) and T3 (system/workflow design) are separate. The critical-thinking perspective includes P1.2, R2.2, R3.2 and R4.1–R4.3 without adding a score. Career views retain disciplinary activities, canonical task links and optional digital contexts. The empty state offers next modules without claiming personal competence or suitability. Missing profile data shows an unavailable message.

A separate actual English C# export (`qa/framework-v2-all-modules-en.json`, SHA256 `038c1b8f521981f79a8b3e551d5e36350ad7e04d547851221b703af12c94db30`) was also loaded: all 16 titles and AI cards, the T2/T3 split, task levels and research-career activities render in English. These are real projection DTOs from synthetic game states, not records of learner achievement.

The settings language control was exercised DE → EN → DE. Welcome, main navigation, journey guide, building controls, dialogs and accessibility labels switch with the host language. `shell-locale.mjs` changes static text nodes and attributes while retaining their controls and icons. A user-entered room called “Bank” remained unchanged during switching and in the room selector. Source questions, answers, descriptions and user-entered names are not rewritten by the UI translator. A second read-only source audit found no material remaining German-only UI copy in the dynamic shell functions.

At the 390 × 844 browser override, the profile dialog measured 283/283 CSS px client/scroll width, the quiz dialog 273/273 and navigation 267/267. The profile, AI cards, career view and new module proposal had no horizontal overflow. The browser override was reset. No browser console errors were observed. These checks establish DOM/transport behaviour; they do not establish a completed or tested Unity 3D world.

The module detail view now reads `module-learning-design.json` for `futureSummary`, proposed objectives, canonical criterion IDs, target levels, context, task, evidence, success criteria, AI role and independent work. The inherited baseline description stays separate. The old `futureDescription` and `learningObjectives` fields are not rendered. A selected option replaces the slot proposal, including the wp/510 case where option and slot share a code. The selector is checked against actual C# all-modules and selected-options fixtures. In the browser, Mentoring 505 displays only its four canonical option objectives (K2.1, K2.3, H2.3, V1.3), in DE and EN. Missing or mismatched proposal data has no legacy fallback. Topic selections remain available and are described as saved planning choices whose concrete task assignment is still open.